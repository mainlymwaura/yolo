---
# Ansible Galaxy requirements for YOLO e-commerce application
# Collections needed for Docker operations

collections:
  - name: community.docker
    version: ">=3.0.0"
  - name: ansible.posix
    version: ">=1.4.0"
